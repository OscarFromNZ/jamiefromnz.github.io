class SandboxGame extends Game {
    constructor(canvasId) {
        super(canvasId);
        // Initialization specific to sandbox mode
    }

    restart() {
        // Restart logic specific to sandbox mode
    }

    // Other methods specific to sandbox mode
}